Strategies {
        // for rrd charts, we need a cron strategy, every Minute is a must have.
    everyMinute : "0 * * * * ?"
        // get the data reduced for older values to keep database small
    everyHour : "0 0 * * * ?"
    everyDay : "0 0 0 * * ?"
    default = everyUpdate
}
Items {
   // * : strategy = everyUpdate, restoreOnStartup

    // conservatory_Heat_Mode                : strategy = everyUpdate, restoreOnStartup
    // //CT_Heating_PresetNormal_Group* : strategy = everyUpdate, restoreOnStartup
    // conservatory_TemperatureSetpoint                : strategy = everyUpdate, restoreOnStartup
    // conservatory_Temperature : strategy = everyMinute, restoreOnStartup
    // conservatory_Humidity : strategy = everyMinute, restoreOnStartup
    // conservatory_Heater : strategy = everyMinute, restoreOnStartup
    
    // frontRoom_Heating_Mode                : strategy = everyUpdate, restoreOnStartup
    // //FR_Heating_PresetNormal_Group* : strategy = everyUpdate, restoreOnStartup
    // frontRoom_TargetTemperature                : strategy = everyUpdate, restoreOnStartup
    // frontRoom_CurrentTemperature : strategy = everyMinute, restoreOnStartup
    // frontRoom_CurrentHumidity : strategy = everyMinute, restoreOnStartup
    // frontRoom_Heating : strategy = everyMinute, restoreOnStartup
}