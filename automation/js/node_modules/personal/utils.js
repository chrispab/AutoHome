exports.showItem = function(data,logger ) {
    var logger = (logger) ? logger : log('sendAlert');
    logger.warn('ALERT: ' + data);
    // actions.NotificationAction.sendBroadcastNotification(message, 'alarm', 'alert');
  //  if(!actions.Things.getActions("mail", "mail:smtp:gmail").sendMail("email@server.com", "openHAB Alert", message)) {
  //    logger.error("Failed to send email alert alert");
  //  }
  // console.error(
    logger.warn(

    '-----showItem data info: DATA.itemName: ' + data.itemName
    + ', LABEL: ' + items.getItem(data.itemName).label +
      ', STATE: ' +      items.getItem(data.itemName).state +
      ', PREV STATE: ' +      items.getItem(data.itemName).history.previousState()
  );
  }
